export const metadata = {
  title: "Deye Load Shedding Monitor V6",
  description: "Deye Cloud Load Shedding Monitor",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}